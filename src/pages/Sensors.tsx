import TableApp from '../shared/components/TableApp';

const Sensors = () => {
  return (
		<div style={{ height: 300, width: '100%' }}>
			<TableApp />
		</div>
	);
}

export default Sensors